/**
 * Schema Drizzle do SafeCircle.
 *
 * Phase 0 — Fundação: a integração PostgreSQL + Drizzle está configurada, mas o
 * modelo de domínio (User, TrustedGroup, EmergencyAlert, ...) NÃO é criado ainda.
 * As tabelas serão adicionadas nas fases correspondentes, cada uma com sua migration.
 *
 * Mantemos este módulo como ponto único de definição de schema para o drizzle-kit.
 */

export {};
