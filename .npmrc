# Metro (Expo/React Native) não lida bem com a árvore simbólica padrão do pnpm.
# node-linker=hoisted gera um node_modules plano, evitando falhas de resolução.
node-linker=hoisted
strict-peer-dependencies=false
